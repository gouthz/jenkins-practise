pipeline {
        agent any
     
        
        stages {
            // Add jenkins environment variables to configuration
            stage('Env') {
                steps {
                    script {
                        sh 'ls'
                        echo "Hello"
                    }
                }
            }
        }
}
